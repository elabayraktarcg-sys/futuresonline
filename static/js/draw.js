/* draw.js — stub
   TradingView Advanced Chart widget kendi çizim araçlarını içerdiğinden
   bu modül artık aktif değildir. API uyumluluğu için boş kalır.
*/
const DRAW = (() => {
  function selectTool() {}
  function init()       {}
  function undo()       {}
  function clearAll()   {}
  function toolbarHTML(){ return ''; }
  return { selectTool, init, undo, clearAll, toolbarHTML };
})();
